# History

One line per retired slice, oldest first. Detail lives in the completed slice file; this is the narrative index.

Format: `YYYY-MM-DD - NNN title - what shipped - ADRs touched - divergence from plan`

2026-09-17 - 001 Set up the Python toolchain and the check gate - `make check` running ruff then pytest, green in CI on a clean clone - 0001, 0005, neither changed - ruff 0.16 formats Markdown so `*.md` is excluded, and `setup-uv` has no `v10` tag so it is pinned to `v10.1.0`
2026-09-17 - 002 Deploy the application behind a link token - a Next.js placeholder on Vercel gated by a path token, refusing with a page, plus `standards/nextjs.md` and `tsc` and `eslint` in the gate - 0002, 0005, 0006, none changed - the gate is `proxy.ts` because Next 16 renamed middleware, the token is a real route segment rather than a rewrite, and `vercel.json` was needed to stop Vercel building the repository as a Python project
2026-09-17 - 003 Create the schema and the migration runner - seven tables in Neon, applied in numeric order by `demogym migrate` with a ledger and indexes drawn from the specification - 0002, 0003, neither changed - one migration file per table so the ordering is load-bearing, the ledger is created by the runner rather than by a migration, and bands are stored lower case and capitalised for display
2026-09-17 - 004 Generate and seed the estate - 6 sites, 300 members and 12,110 entries in Neon from a committed generator, with 14 invariant tests each checked by breaking the rule it guards - 0003, 0005, neither changed - arrivals are generated in Europe/London and stored as timestamptz, seeding replaces rather than appends, and a leaving-date bug found by the invariants put 7 entries on members' leaving dates
2026-09-17 - 005 Generate and seed the equipment - 100 units across the six sites from a 48-type catalogue written by rule, six out of service with two long-standing faults, and 21 invariant tests each checked by breaking the rule it guards - 0003, unchanged - fault ages are drawn in two groups so a long-standing fault is reliable rather than lucky, and the mutation run exposed a tautological status test that compared the module against itself
2026-09-17 - 006 Score the estate - 3,010 scores across twelve weekly dates with nineteen High at the most recent, every cut point tested and each checked by moving its threshold, and three rows recomputed by hand - 0004, 0005, neither changed; the specification gained one open question - a row exists for every eligible member so migration 008 admits an unflagged band, the gap reading has no minimum-data guard, and 004 was reopened to give members spaced training days rather than an independent draw each morning
2026-09-17 - 007 Build the application shell and the visual design - the shell, the synthetic-data strip, the cost readout and the shared primitives and states, with an interface reference page, measured in headless Chrome for overflow, focus rings and reduced motion - 0006, 0008, neither changed - the first banner tint measured 1.03:1 and became a solid dark strip, the daily limit is styled as a limit rather than a fault, and screenshots caught three things the markup did not
2026-09-17 - 008 Build the estate screen - six sites read from Neon in one query, ranked by at-risk rate, with a twelve-point trend beside the change against last week, deployed and hand-checked row by row - 0002, 0004, 0005, none changed - the sparklines share one estate-wide scale that does not start at zero, the at-risk rate divides by members scored rather than active, and the columns were reordered after the rate turned out to be off-screen at phone width
2026-09-17 - 009 Build the at-risk queue - 46 banded members on one screen, worst first and by price inside a band, filterable by site, deployed and checked against the estate counts - 0004, 0005, neither changed - five reason-string defects surfaced only once forty-six of them sat in a column, four of them in the scorer and fixed with tests, and the estate was rescored to pick up the wording
2026-09-17 - 010 Build the drafting endpoint - a deployed Python function drafting one member from eight computed facts and three dropdown settings, with the attempt limit, the daily cap and the three failure states, proved against the live database and the real API - 0003, 0004, 0005, 0006, 0007, 0008, none changed; the specification gained an eighth table - a call takes about seven seconds rather than the couple 0008 estimates and two thirds of its output tokens are reasoning, the first version returned the provider's raw error body to the reader, and a truncated message reads as unreachable rather than earning a fourth failure state
2026-09-17 - 011 Draft from the queue - one set of controls, Draft all four calls at a time, redraft, the four row states and the cost readout wired to the stored token counts, driven in a real browser against the deployed function - 0002, 0005, 0006, 0008, none changed; the specification gained an open question - the High band became panels because a message does not fit in a table cell, the cost is shown in US cents rather than converted at a rate nobody measured, the slice gained a local development path for the endpoint, and putting a reason beside a drafted message exposed a scorer wording defect and a scoring rule that misfires on a joiner
2026-09-17 - 006 Score the estate, amended after 011 - the no-visits rule now fires on the trailing 12 weeks or since joining rather than on the baseline window, and the typical gap is measured over the same period - none touched; the specification's rule text and typical-gap definition were rewritten and one open question closed - the fix needed two parts because changing the rule alone would have left a joiner with no gap reading at all, and 24 of 3,010 rows changed band with High moving from 19 to 18
2026-09-17 - 012 Record decisions on a draft - Approve, Edit and Reject on a drafted member, the edit marked inside the message rather than printed beside it, "Approved, not sent" on the row, and the edit share in the header, all driven against the deployed endpoint - 0004, 0005, 0006, 0008, none changed - a decision belongs to the member rather than to an attempt so drafting is refused afterwards, the first edit view printed the message twice and a screenshot caught it, and the HTTP glue moved into one shared class rather than being copied for the second endpoint
2026-09-17 - 013 Build the site briefing - a briefing per site written on request from computed facts, with new slot arithmetic, a projection whose horizon is a stated assumption, and eleven links across six briefings none of which claims a cause - 0004, 0005, 0007, 0008, none changed; the specification's open question about invalidating a briefing is closed - the slot windows deliberately do not overlap where the decay ratio's do, regenerating adds the tokens so a billed call cannot vanish from the running total, and reading the output found two prompt defects that reading the prompt would not have
2026-09-18 - 014 Write the running-on-real-data page - eight numbered sections carrying every limitation the specification records, the four feeds a real deployment reads, the regulatory position and the first week, reading nothing from the database and checked on the deployment - 0005, 0006, 0007, 0008, none changed - the section rules ran across empty paper until a screenshot showed it, the focus-ring check was wrong before the ring was because a scripted focus does not match `:focus-visible`, and the deployment check needed `.env` rather than `.env.local`
